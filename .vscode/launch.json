{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Transaction API",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--port", "8083"],
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
